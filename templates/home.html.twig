{% extends 'base.html.twig' %}


{% block body %}

<div class="container">
  <div class="row">
    
    <div class="col-sm">
     {% for el in content %}
        <p>{{ el.stationName}}</p>
    {% endfor %}
    </div>
    
  </div>
</div>

{% endblock %}